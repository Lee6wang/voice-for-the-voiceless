# 无声之声 · 接口契约与开发规格

> 本文件是**开发时天天看、天天更新的活文档**（区别于对外的项目方案）。
> 目的：把 `glasses-app`（眼镜插件）/ `config-app`（手机配置）/ `backend`（薄后端）三方的边界钉死，让两人并行开发在联调时不炸。
> 版本：v0.1（草案，两人过一遍达成一致后开工）

---

## 0. 模块与归属

| 模块 | 归属 | 技术栈 | 职责 |
|---|---|---|---|
| `glasses-app` | A | TS + Even Hub SDK | 采 PCM、收输入事件、渲染 HUD、编排云 API、播 TTS、选择状态机 |
| `config-app` | B | 手机 App（简单表单即可） | 个性化配置录入 → 写 `backend` |
| `backend` | B | serverless / 小 Node | 配置读写 + 云 API 密钥代理（转发 ASR/LLM/TTS） |

> 说明：按 Even Hub 架构，`glasses-app` 是跑在手机上的 Web 插件，眼镜只是它的屏幕+输入。云 API **一律经 `backend` 代理**（密钥不落客户端）。

---

## 1. 数据模型（TypeScript 类型，三方共用）

```ts
// 一条候选回复
interface Candidate {
  id: string;        // 稳定 id，供选择/埋点
  text: string;      // 展示&朗读文本，≤12 字
}

// 一屏候选（固定 4 条）
interface CandidateSet {
  turnId: string;        // 对应哪一轮对方发言
  heardText: string;     // ASR 转写的“对方说了什么”（可显示在 HUD 顶部）
  candidates: Candidate[]; // 长度固定 4
  highlightIndex: number;  // 当前高亮项，默认 0
}

// 输入事件（来自 R1 戒指 / 镜腿触控，Even SDK 原生事件）
// 语义随状态而变，见 §2 状态机
type RawInput =
  | 'tap'           // IDLE: 开始聆听 ; 候选态: 确认选中→朗读
  | 'swipe_up'      // 候选态: 上一个候选
  | 'swipe_down'    // 候选态: 下一个候选
  | 'double_tap'    // IDLE: 唤主动模式 ; 候选态: 换一批
  | 'temple_double_tap'; // 任意状态: 紧急呼救（镜腿独立表面）

// 用户个性化配置
interface UserProfile {
  userId: string;
  name?: string;             // 用于候选里的自我介绍
  commonPhrases: string[];   // 常用语，喂进 LLM prompt
  tone?: 'gentle' | 'plain' | 'humor'; // 语气偏好
  voice?: string;            // TTS 音色 id
  emergencyText?: string;    // 紧急呼救要喊的话
}
```

---

## 2. glasses-app 主流程与状态机（A 负责）

**触发模型：push-to-listen（按戒指开始听，非常开）**

```
状态机：
IDLE ──tap──→ LISTENING(采音 3~5s) ──→ THINKING(调 /asr /candidates)
                                              │
                                              ↓
 IDLE ←─tap确认后─ SPEAKING(播 TTS,静音麦) ←─ CANDIDATES(HUD 显 4 候选)
                                              │ swipe 导航 / double_tap 换一批
 IDLE ──double_tap─→ 主动模式轮盘 → CANDIDATES
 任意状态 ──temple_double_tap─→ 紧急：直接播 profile.emergencyText
```

- **防回声**：只在 LISTENING 采音；SPEAKING 期间静音麦克风——杜绝“听到自己”的死循环。
- **HUD 分段反馈**：LISTENING显“🎧聆听中…”、THINKING 显“💭思考中…”（候选要 2~4s，不显会像卡死）。
- **确认两步制**（防误触，可选）：候选态首次 tap 选中高亮项，再 tap 才朗读（Day2 视误触率启用）。

---

## 3. backend HTTP 接口（B 负责，`glasses-app` 调用）

> 全部 POST，JSON。`backend` 内部再去调各家云 API（密钥只在 backend）。

### 3.1 `POST /asr` — 语音转文字
```
请求:  { "audio": "<base64 PCM 16kHz mono>", "final": true }
返回:  { "text": "对方说的话" }
```
- 流式版可选：`/asr/stream`（WebSocket），Day2 有余量再做；Day1 先用整段。
- 当前实现：sherpa-onnx + SenseVoice 端侧离线识别（免密钥，断网可用），模型下载见 `backend/models/README.md`。
- 错误返回（均为 `{ "ok": false, "error": "..." }`）：400 = 缺 `audio`；503 = 模型未下载；500 = 识别异常。前端按识别失败处理（提示对方重说一遍），不白屏。

### 3.2 `POST /candidates` — 生成候选（核心）
```
请求:  {
  "turnId": "t_123",
  "heardText": "你中午想吃什么？",
  "profile": { ...UserProfile },
  "exclude": ["面条","米饭"]   // 换一批时传，避免重复
}
返回:  {
  "turnId": "t_123",
  "candidates": [ {"id":"c1","text":"面条"}, ... 共4条 ]
}
```
- backend 内部：拼 prompt（注入 name/commonPhrases/tone）→ 调云 LLM → 解析出 4 条 ≤12 字。
- **双层兜底**（关键，两者不同）：
  - ① **backend 侧**：backend 可达但 LLM 超时/报错 → backend 返回模板库候选。
  - ② **插件端（客户端）**：网络/backend 本身不可达（断网）→ glasses-app 用**内置的本地模板库**出候选。⚠️ 断网时 backend 也够不着，所以“无网也能演”的模板库**必须在插件端**，不能只放 backend。
- 模板库内容见方案 §附录 A；两处共用同一份（可从 backend 同步到插件内置）。无论哪层，保证永远返回 4 条。

### 3.3 `POST /tts` — 文字转语音
```
请求:  { "text": "我想吃面条", "voice": "default" }
返回:  { "audio": "<base64 mp3>", "mime": "audio/mpeg" }    // glasses-app 播到手机扬声器
```
- `mime` 为可选字段（向后兼容），缺省按 mp3 处理；当前实现恒为 `audio/mpeg`（24kHz mono）。
- `voice` 可选值：`zh-CN-XiaoxiaoNeural`(女/默认)、`zh-CN-YunxiNeural`(男)、`zh-CN-YunyangNeural`(男播音)、`zh-CN-XiaoyiNeural`(女柔)；传 `default` 或未知值落默认音色。
- 同一 `text+voice` 在 backend 磁盘缓存；断网时已合成过的句子（如紧急表达）仍可返回。
- 合成失败且无缓存：返回 `{ "audio": "" }`，前端降级为 HUD 纯文字展示。

### 3.4 `GET /profile?userId=` / `POST /profile` — 配置读写
```
GET  返回: { ...UserProfile }
POST 请求: { ...UserProfile } → { "ok": true }
```
- `config-app` 写，`glasses-app` 每次会话开始时读一次（或缓存）。

---

## 4. config-app（B 负责）

- 一个极简表单：名字、常用语（多行）、语气（三选一）、紧急呼救语、（可选）音色。
- 保存 = `POST backend /profile`。
- **不追求复杂账户系统**：userId 用一个固定值或二维码绑定即可（Demo 够用）。

---

## 5. 契约约定（避免联调翻车）

1. **候选恒为 4 条**、每条 `text ≤ 12 字`；不足由 backend 补兜底句。
2. **turnId** 全程透传，用于把"对方这句话"和"这批候选"对齐，避免串话。
3. 所有云调用**只走 backend**；`glasses-app` 不持有任何密钥。
4. 音频编码统一：PCM 上行 16kHz mono base64；TTS 下行 mp3/wav base64。
5. backend 任何异常都要返回**可渲染的兜底**（4 条通用候选 / 一段兜底 TTS），前端不白屏。

---

## 6. Day 1 起步清单（可勾选）

**A（glasses-app）**
- [ ] 模拟器跑通：HUD 渲染 4 条候选 + 高亮
- [ ] 接 R1/镜腿事件 + 状态机：tap(IDLE)开始聆听 / swipe 移高亮 / tap(候选态)确认 / double_tap 换批 / 镜腿紧急
- [ ] push-to-listen：tap 触发采音 3~5s；SPEAKING 时静音麦（防回声）；LISTENING/THINKING 显分段反馈
- [ ] 用假数据（写死的 CandidateSet）打通"选中→播一段 WAV 到扬声器"
- [ ] 接 backend `/candidates`、`/tts`（先用 B 的 mock）
- [ ] 内置本地模板库（断网/backend 不可达时在插件端出候选，保无网可演）

**B（backend + config-app）**
- [ ] 起 backend 骨架，先 mock 返回固定 4 候选 + 一段 TTS，让 A 能联调
- [ ] 接一家云 ASR / LLM / TTS，`/asr` `/candidates` `/tts` 跑通
- [ ] `/candidates` 注入 profile 到 prompt；backend 侧模板库兜底（LLM 超时时）
- [ ] config-app 表单 → `/profile` 读写

**联调点（Day 2 上午）**：A 用真 backend 跑通"说话→候选上屏→选中→发声"，实测 ≤5s。

---

## 7. 待定 / Day 0 验证（见方案 §5.2）
- [ ] 插件能否播音频到手机扬声器（不行则由 config-app 播）
- [ ] 插件 fetch 云/backend 的网络与跨域
- [ ] 端到端延迟 ≤5s
